# DEPRECATED name kept for back-compat. The reproduction locks are now split:
#   requirements-cpu.lock  -- tests + every paper figure (CPU-only)
#   requirements-gpu.lock  -- adds torch for the supplement-S6 batched MC
# The pins below equal requirements-cpu.lock so old references still install a
# complete CPU environment.
numpy==2.4.6
scipy==1.17.1
matplotlib==3.10.9
networkx==3.6.1
pandas==2.3.3
pytest==9.0.3

# GPU tier (see requirements-gpu.lock): torch==2.8.0 (+cu128, CUDA 12.8, RTX
# 4080 SUPER, driver 595.71.05) + psutil==5.9.8. Install via
# `pip install -e '.[gpu]'` or `-r requirements-gpu.lock` on a CUDA machine.
# cvxpy is OPTIONAL (lazy import in src/tfl/estimators.py; no test/run_all path).
